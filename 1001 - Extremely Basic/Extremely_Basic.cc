#include <iostream>

using namespace std;

int main()
{
    // Declare variables
    int A, B;

    // Read inputs A and B
    cin >> A >> B;

    // Calculate the sum
    int X = A + B;

    // Print the result
    cout << "X = " << X << endl;

    return 0;
}
