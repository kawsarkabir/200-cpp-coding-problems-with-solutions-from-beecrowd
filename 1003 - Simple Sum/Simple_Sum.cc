#include <iostream>

int main()
{
    int A, B;
    std::cin >> A >> B; // Input the values of A and B

    int SOMA = A + B; // Calculate the sum

    // Output the result
    std::cout << "SOMA = " << SOMA << std::endl;

    return 0;
}
