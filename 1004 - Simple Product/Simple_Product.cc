#include <iostream>

int main()
{
    int A, B;
    std::cin >> A >> B; // Input the values of A and B

    int PROD = A * B; // Calculate the Product

    // Output the result
    std::cout << "PROD = " << PROD << std::endl;

    return 0;
}
